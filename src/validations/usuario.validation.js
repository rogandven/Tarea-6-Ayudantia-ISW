"use strict";
import Joi from "joi";

export const usuarioBodyValidation = Joi.object({
    id: Joi.string().
    email:
    password:
    created_at:
    updated_at: 
});

export const usuarioQueryValidation = Joi.object({

});
